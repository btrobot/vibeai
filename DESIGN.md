# DESIGN.md

> VibeAI 内容创作平台 — 设计规范唯一真相源
> 参考：Replicate（内容优先的卡片网格）、Penpot（系统化组件思维）

---

## 1. 产品定位与用户画像

### 产品
**VibeAI** — AI 视频/图片生成 + 电商内容工具 + 后台管理平台。用户在此提交生成任务、管理作品、消耗信用额度、浏览社区画廊。

### 用户画像
| 角色 | 核心诉求 | 设计影响 |
|------|---------|---------|
| 内容创作者 | 快速出图、迭代试错、作品管理 | 生成面板要高效，画廊网格要沉浸 |
| 电商运营 | 批量生成、素材管理、成本可控 | 用量统计清晰，信用消耗透明 |
| 设计师 | 精细控制、风格一致性、导出灵活 | 参数面板完整，作品库可检索 |

### 气质关键词
明亮 · 干净 · 专业 · 呼吸感 · 内容优先

---

## 2. 意象锚点

**清晨的工作台** — 阳光透过百叶窗洒在白色桌面上，MacBook 屏幕亮着，旁边放着一杯美式咖啡和一本 Moleskine 笔记本。桌面整洁，只留当前任务需要的东西。

设计推论：
- 大面积留白 → 内容区域不被 UI chrome 干扰
- 自然光 → 低对比度的中性色，不刺眼
- 桌面整洁 → 信息密度适中，层级清晰
- 只留当前任务 → 每个页面聚焦一个核心操作

---

## 3. 视觉策略

### 核心原则：内容优先（参考 Replicate）
> 用户来 VibeAI 是看 AI 生成的作品，不是看 UI。界面应当像画廊的白墙——存在但不抢眼。

- **大面积留白**：内容区域充足留白，让作品本身成为焦点
- **轻量层级**：用极浅灰区分卡片与背景，不用重阴影
- **色彩克制**：主色用于关键操作（生成、保存、发布），其余保持中性
- **图像方向**：高亮度、自然色调、适度对比度
- **消失的 UI**：导航、工具栏在非交互时保持极低视觉权重

### 图形语言
- 直线条为主，圆角柔和但不圆润
- 无装饰性插画，用真实生成内容填充视觉
- 图标统一使用 Lucide（线性风格，1.5px stroke）
- 数据可视化用最小化图表（无网格线、无图例框）

---

## 4. 配色方案

### 4.1 基础色

| 用途 | CSS 变量 | HSL 值 | Hex 参考 | 说明 |
|------|---------|--------|---------|------|
| 背景底色 | `--background` | `hsl(0 0% 98%)` | `#FAFAFA` | 极浅灰白，不刺眼 |
| 表面色（卡片） | `--card` | `hsl(0 0% 100%)` | `#FFFFFF` | 纯白 |
| 表面色（悬浮） | `--surface-hover` | `hsl(220 14% 96%)` | `#F4F4F5` | 浅灰，hover 状态 |
| 次要表面 | `--secondary` | `hsl(220 14% 96%)` | `#F4F4F5` | 次级容器背景 |
| 弱化表面 | `--muted` | `hsl(220 14% 96%)` | `#F4F4F5` | 静音/禁用容器 |
| 边框/分隔线 | `--border` | `hsl(220 13% 91%)` | `#E4E4E7` | 极浅灰，不抢眼 |
| 输入框边框 | `--input` | `hsl(220 13% 91%)` | `#E4E4E7` | 同边框 |

### 4.2 文字色

| 用途 | CSS 变量 | HSL 值 | Hex 参考 | 说明 |
|------|---------|--------|---------|------|
| 主文字 | `--foreground` | `hsl(224 71% 4%)` | `#09090B` | 深灰黑，高可读 |
| 卡片文字 | `--card-foreground` | `hsl(224 71% 4%)` | `#09090B` | 同主文字 |
| 次要文字 | `--muted-foreground` | `hsl(220 9% 46%)` | `#71717A` | 中灰，辅助信息 |
| 次要表面文字 | `--secondary-foreground` | `hsl(224 71% 4%)` | `#09090B` | 深灰黑 |

### 4.3 主色（品牌色 — 专业蓝）

| 用途 | CSS 变量 | HSL 值 | Hex 参考 | 说明 |
|------|---------|--------|---------|------|
| 主色 | `--primary` | `hsl(221 83% 53%)` | `#2563EB` | 专业蓝，信任感 |
| 主色文字 | `--primary-foreground` | `hsl(0 0% 100%)` | `#FFFFFF` | 白色 |
| 主色悬浮 | — | `hsl(221 83% 45%)` | `#1D4ED8` | 深蓝，hover |
| 主色浅底 | — | `hsl(221 83% 95%)` | `#EFF6FF` | 极浅蓝，选中/激活背景 |
| 焦点环 | `--ring` | `hsl(221 83% 53%)` | `#2563EB` | 同主色 |

### 4.4 强调色（翡翠绿 — 品牌标识）

| 用途 | CSS 变量 | HSL 值 | Hex 参考 | 说明 |
|------|---------|--------|---------|------|
| 强调色 | `--accent` | `hsl(160 40% 40%)` | `#3D9970` | 仅用于 Logo、"生成"按钮等品牌元素 |
| 强调色文字 | `--accent-foreground` | `hsl(0 0% 100%)` | `#FFFFFF` | 白色 |
| 强调色悬浮 | — | `hsl(160 50% 50%)` | `#33CC95` | hover 状态 |
| 强调色浅底 | — | `hsl(160 40% 95%)` | `#EAF7F1` | 极浅绿，徽章背景 |

### 4.5 语义色

| 用途 | HSL 值 | Hex 参考 | 使用场景 |
|------|--------|---------|---------|
| 成功 | `hsl(142 76% 36%)` | `#22C55E` | 任务完成、保存成功、信用充足 |
| 警告 | `hsl(38 92% 50%)` | `#F59E0B` | 信用不足、即将过期、待确认 |
| 危险/错误 | `hsl(0 84% 60%)` | `#EF4444` | 删除、失败、信用耗尽 |
| 信息 | `hsl(221 83% 53%)` | `#2563EB` | 同主色，提示信息 |

### 4.6 图表色板

| 变量 | HSL 值 | 用途 |
|------|--------|------|
| `--chart-1` | `hsl(221 83% 53%)` | 主数据系列（蓝） |
| `--chart-2` | `hsl(160 40% 40%)` | 次数据系列（绿） |
| `--chart-3` | `hsl(38 92% 50%)` | 第三系列（橙） |
| `--chart-4` | `hsl(280 60% 55%)` | 第四系列（紫） |
| `--chart-5` | `hsl(0 84% 60%)` | 第五系列（红） |

### 4.7 色彩使用边界

- **主色（蓝）**：导航激活、链接、主要按钮、焦点环、选中状态、标签页激活
- **强调色（绿）**：Logo 图标、"生成"核心操作按钮、品牌标识、成功状态徽章
- **禁止混用**：导航栏激活状态用蓝色，不用绿色；"生成"按钮用绿色，不用蓝色
- **语义色不混用**：成功只用绿色语义色，不用翡翠绿强调色（两者 HSL 不同）

---

## 5. 字体排版

### 字体族

| 用途 | 字体 | CSS 变量 | 来源 |
|------|------|---------|------|
| 英文/数字 | Inter | `--font-sans` | Google Fonts (`.cn` 域) |
| 中文 | PingFang SC / Microsoft YaHei | 系统默认 | fallback |
| 等宽 | JetBrains Mono | — | 代码、数据、信用数值 |

### 字号层级

| 层级 | 大小 | 行高 | 字重 | 字间距 | Tailwind 映射 | 用途 |
|------|------|------|------|--------|-------------|------|
| Display | 36px | 1.2 | 700 | -0.02em | `text-4xl` | 首页大标题 |
| H1 | 28px | 1.3 | 700 | -0.01em | `text-3xl` | 页面标题 |
| H2 | 20px | 1.4 | 600 | -0.01em | `text-xl` | 区块标题 |
| H3 | 16px | 1.5 | 600 | 0 | `text-base` | 卡片标题、小标题 |
| Body | 14px | 1.6 | 400 | 0 | `text-sm` | 正文（默认） |
| Small | 12px | 1.5 | 400 | 0 | `text-xs` | 辅助文字（最小） |

### 排版规则

- 正文默认 14px，不使用 16px（信息密度优先）
- 数字和代码使用等宽字体，确保对齐
- 标题使用 `font-weight: 600-700`，不用 800+（过重破坏轻盈感）
- 中文与英文/数字之间自动添加 `0.25em` 间距（通过 `word-spacing` 或手动空格）
- 长文本段落最大宽度 `65ch`（约 65 个字符），确保可读性

---

## 6. 间距系统

基于 **4px 网格**，使用 Tailwind 默认刻度：

| Token | 值 | Tailwind | 用途 |
|-------|-----|---------|------|
| xs | 4px | `p-1` | 图标与文字间距、紧凑内边距 |
| sm | 8px | `p-2` | 按钮内边距、标签间距 |
| md | 16px | `p-4` | 卡片内边距、表单项间距 |
| lg | 24px | `p-6` | 区块间距、卡片间距 |
| xl | 32px | `p-8` | 页面分区间距 |
| 2xl | 48px | `p-12` | 大区块间距 |
| 3xl | 64px | `p-16` | 页面顶部/底部留白 |

### 页面布局间距
- 页面左右边距：`24px`（移动端）/ `48px`（桌面端）
- 内容最大宽度：`1200px`（标准）/ `1440px`（宽屏画廊）
- 侧边栏宽度：`240px`（收起 `64px`）
- 卡片间距：`16px`（网格内）/ `24px`（区块间）

---

## 7. 圆角与阴影

### 圆角

| 用途 | 值 | Tailwind | CSS 变量 |
|------|-----|---------|---------|
| 按钮、输入框、标签 | 8px | `rounded-lg` | `--radius` |
| 卡片、面板 | 12px | `rounded-xl` | — |
| 弹窗、模态框 | 16px | `rounded-2xl` | — |
| 头像、圆形元素 | 50% | `rounded-full` | — |
| 图片缩略图 | 8px | `rounded-lg` | — |
| 徽章、小标签 | 4px | `rounded` | — |

### 阴影

| 层级 | 值 | 用途 |
|------|-----|------|
| 无 | `none` | 默认卡片（用边框区分，不用阴影） |
| 小 | `0 1px 2px hsl(0 0% 0% / 0.05)` | 按钮、输入框悬浮 |
| 中 | `0 4px 6px -1px hsl(0 0% 0% / 0.1)` | 卡片悬浮、下拉菜单 |
| 大 | `0 20px 25px -5px hsl(0 0% 0% / 0.1)` | 弹窗、模态框 |
| 最大 | `0 25px 50px -12px hsl(0 0% 0% / 0.25)` | 全屏遮罩上的浮动层 |

### 阴影使用原则
- **默认不用阴影**：卡片靠 1px 边框区分层级
- **悬浮才加阴影**：hover 时提升到「中」级阴影
- **弹窗用大阴影**：模态框用「大」级，确保层级感知

---

## 8. 动效与交互

### 过渡参数

| 类型 | 时长 | 缓动函数 | 用途 |
|------|------|---------|------|
| 快速 | 150ms | `cubic-bezier(0.4, 0, 0.2, 1)` | 按钮、输入框、颜色切换 |
| 正常 | 200ms | `cubic-bezier(0.4, 0, 0.2, 1)` | 卡片悬浮、面板展开 |
| 慢速 | 300ms | `cubic-bezier(0.4, 0, 0.2, 1)` | 页面切换、弹窗、抽屉 |

### 交互状态

| 状态 | 视觉变化 | 实现 |
|------|---------|------|
| 默认 | 基础样式 | — |
| 悬停 | 背景变浅灰 + 阴影提升 | `hover:bg-muted hover:shadow-md` |
| 点击 | 轻微缩放 | `active:scale-[0.98]` |
| 聚焦 | 2px 主色外环 | `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` |
| 禁用 | 50% 透明度 + 禁止光标 | `disabled:opacity-50 disabled:cursor-not-allowed` |
| 加载 | 旋转图标或骨架屏 | `Spinner` 或 `Skeleton` 组件 |

### 动效禁忌
- 禁止无意义的旋转、弹跳动画
- 禁止 `linear` 缓动（机械感）
- 禁止超过 400ms 的过渡（感觉迟钝）
- 加载动画不阻塞交互（乐观更新）

---

## 9. 布局模式

### 9.1 应用框架（已登录）

```
┌──────────────────────────────────────────────┐
│  TopBar (56px)  Logo · Search · User Menu    │
├────────┬─────────────────────────────────────┤
│        │                                     │
│ Side   │         Main Content                │
│ nav    │       (max-w: 1200px)               │
│ (240px)│                                     │
│        │                                     │
├────────┴─────────────────────────────────────┤
│  (无底部栏)                                    │
└──────────────────────────────────────────────┘
```

- **TopBar**：56px 高，白色背景，底部 1px 边框
- **Sidenav**：240px 宽，收起 64px，白色背景，右侧 1px 边框
- **Main**：背景 `--background`（极浅灰），内容区居中

### 9.2 画廊网格（参考 Replicate Explore）

```
┌─────────────────────────────────────┐
│  筛选栏 (tags · sort · view mode)    │
├─────────────────────────────────────┤
│ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐    │
│ │     │ │     │ │     │ │     │    │
│ │ Img │ │ Img │ │ Img │ │ Img │    │
│ │     │ │     │ │     │ │     │    │
│ └─────┘ └─────┘ └─────┘ └─────┘    │
│  title   title   title   title      │
│  author  author  author  author     │
│ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐    │
│ │     │ │     │ │     │ │     │    │
│ ...                                 │
└─────────────────────────────────────┘
```

- **网格列数**：移动端 2 列 / 平板 3 列 / 桌面 4-5 列
- **卡片间距**：16px
- **卡片内容**：图片（aspect-ratio 1:1 或 4:3）+ 标题 + 作者 + 统计
- **无限滚动**：Intersection Observer 触发加载，底部 Skeleton 占位

### 9.3 生成面板（Workspace）

```
┌──────────────────┬──────────────────┐
│  参数面板 (360px) │   预览区 (flex-1)  │
│                  │                  │
│  模型选择         │  ┌────────────┐  │
│  Prompt 输入     │  │            │  │
│  参数滑块         │  │   生成结果   │  │
│  参考图上传       │  │            │  │
│  生成按钮 (绿)    │  └────────────┘  │
│                  │                  │
│  信用消耗提示     │  历史记录 (横向)   │
└──────────────────┴──────────────────┘
```

- 左侧参数面板固定宽度，可折叠
- 右侧预览区自适应，图片居中显示
- 生成按钮使用强调色（翡翠绿），是页面唯一的绿色按钮
- 信用消耗在按钮旁实时显示

### 9.4 仪表盘

```
┌─────────────────────────────────────┐
│  欢迎语 + 快捷操作                    │
├──────────┬──────────┬──────────┬─────┤
│ 生成次数  │ 信用余额  │ 存储用量  │ 项目 │
├──────────┴──────────┴──────────┴─────┤
│  最近作品 (横向滚动)                   │
├─────────────────────────────────────┤
│  用量趋势图 (折线图)                   │
└─────────────────────────────────────┘
```

- 统计卡片 4 列，每张含：图标 + 数值 + 标签 + 趋势
- 数值用等宽字体，32px，字重 700
- 趋势用小型 sparkline 或百分比 + 箭头

---

## 10. 组件规范

### 10.1 按钮

| 类型 | 样式 | Tailwind 类名参考 |
|------|------|------------------|
| 主按钮 | 蓝色背景 + 白色文字 | `bg-primary text-primary-foreground hover:bg-primary/90` |
| 生成按钮 | 翡翠绿背景 + 白色文字 | `bg-accent text-accent-foreground hover:bg-accent/90` |
| 次按钮 | 白色背景 + 灰色边框 | `bg-card border border-border hover:bg-muted` |
| 文字按钮 | 透明背景 + 主色文字 | `text-primary hover:text-primary/80 hover:bg-primary/5` |
| 危险按钮 | 红色背景 + 白色文字 | `bg-destructive text-destructive-foreground hover:bg-destructive/90` |
| 幽灵按钮 | 透明背景 + hover 浅灰 | `hover:bg-muted` |

- 高度：`36px`（默认）/ `32px`（紧凑）/ `40px`（大）
- 内边距：`px-4 py-2`（默认）
- 圆角：`rounded-lg`（8px）
- 字号：`text-sm font-medium`

### 10.2 卡片

| 类型 | 用途 | 样式 |
|------|------|------|
| 作品卡 | 画廊网格中的作品 | 白底 + 1px 边框 + 图片 + 元信息 |
| 统计卡 | 仪表盘数据展示 | 白底 + 1px 边框 + 内边距 24px |
| 信息卡 | 设置/详情展示 | 白底 + 1px 边框 + 内边距 24px |
| 悬浮卡 | 可点击的作品卡 | hover 时阴影提升到中级 |

### 10.3 输入框

- 高度：`36px`（默认）
- 背景：白色 `--card`
- 边框：1px `--border`，聚焦时 `--primary`
- 圆角：`rounded-lg`
- 内边距：`px-3 py-2`
- placeholder：`--muted-foreground`
- 错误状态：边框 `--destructive`，下方红色提示文字

### 10.4 标签/徽章

| 类型 | 样式 | 用途 |
|------|------|------|
| 默认 | 浅灰背景 + 深灰文字 | 分类、状态 |
| 主色 | 主色浅底 + 主色文字 | 选中、激活 |
| 成功 | 绿色浅底 + 绿色文字 | 完成、在线 |
| 警告 | 橙色浅底 + 橙色文字 | 待处理、低信用 |
| 危险 | 红色浅底 + 红色文字 | 失败、错误 |

- 圆角：`rounded`（4px）
- 字号：`text-xs`
- 内边距：`px-2 py-0.5`

### 10.5 表格

- 表头：浅灰背景 `--muted`，文字 `--muted-foreground`，`text-xs font-medium`
- 行高：`48px`
- 行分割线：1px `--border`
- hover 行：背景 `--surface-hover`
- 操作列：右对齐，文字按钮

### 10.6 模态框

- 遮罩：`bg-black/50`（半透明黑）
- 容器：白底 + 16px 圆角 + 大阴影
- 最大宽度：`480px`（小）/ `640px`（中）/ `800px`（大）
- 内边距：`24px`
- 关闭按钮：右上角，幽灵按钮风格
- 动画：淡入 + 轻微上滑（`translateY(8px)` → `0`，300ms）

### 10.7 抽屉

- 从右侧滑入，宽度 `400px`（默认）
- 背景白色，左侧 1px 边框 + 中级阴影
- 滑入动画：`translateX(100%)` → `0`，300ms

### 10.8 进度条

- 高度：`8px`（默认）/ `4px`（细）
- 背景：`--muted`
- 填充：`--primary`（默认）/ `--accent`（生成进度）
- 圆角：`rounded-full`
- 动画：宽度过渡 300ms

### 10.9 骨架屏

- 背景：`--muted`
- 闪烁动画：`animate-pulse`（Tailwind 内置）
- 形状：匹配实际内容尺寸（矩形、圆形）
- 用于：图片加载、数据请求、表格行

---

## 11. 响应式断点

| 断点 | 宽度 | Tailwind | 布局变化 |
|------|------|---------|---------|
| mobile | < 640px | 默认 | 单列、侧边栏隐藏（汉堡菜单）、底部导航 |
| sm | ≥ 640px | `sm:` | 2 列网格 |
| md | ≥ 768px | `md:` | 3 列网格、侧边栏可展开 |
| lg | ≥ 1024px | `lg:` | 4 列网格、侧边栏固定显示 |
| xl | ≥ 1280px | `xl:` | 5 列网格、参数面板展开 |
| 2xl | ≥ 1536px | `2xl:` | 宽屏画廊 6 列 |

### 移动端适配
- 侧边栏变为底部 Tab Bar 或汉堡菜单
- 生成面板参数区变为底部抽屉
- 表格变为卡片列表
- 模态框变为全屏

---

## 12. 图标规范

- **图标库**：Lucide React（线性风格）
- **默认大小**：`20px`（`size={20}`）
- **小图标**：`16px`（`size={16}`），用于标签、徽章
- **大图标**：`24px`（`size={24}`），用于空状态、引导
- **stroke 宽度**：`1.5px`（默认）
- **颜色**：继承文字颜色 `currentColor`
- **禁止**：使用填充图标（filled）、彩色图标、emoji 替代图标

---

## 13. 数据展示模式

### 13.1 任务状态

| 状态 | 颜色 | 图标 | 文案 |
|------|------|------|------|
| 排队中 | 灰色 | `Clock` | 排队中 |
| 处理中 | 蓝色 | `Loader2`（旋转） | 生成中... |
| 已完成 | 绿色 | `CheckCircle2` | 已完成 |
| 已失败 | 红色 | `XCircle` | 生成失败 |
| 已取消 | 灰色 | `Ban` | 已取消 |

### 13.2 信用展示

- 余额数字：等宽字体，字重 600
- 消耗提示：按钮旁 `text-xs text-muted-foreground`
- 不足警告：橙色徽章 + 提示文字
- 趋势图：绿色表示消耗，蓝色表示充值

### 13.3 空状态

- 居中显示
- 大图标（48px，`--muted-foreground` 色）
- 标题（H3，`--foreground`）
- 描述文字（Body，`--muted-foreground`）
- CTA 按钮（主按钮）

### 13.4 加载状态

| 场景 | 方案 |
|------|------|
| 页面初次加载 | 骨架屏（匹配布局） |
| 卡片网格 | 底部 Skeleton 行 |
| 按钮提交 | 按钮内 Spinner + 禁用 |
| 表格数据 | Skeleton 行 |
| 全屏操作 | 居中 Spinner + 半透明遮罩 |

---

## 14. 导航规范

### 14.1 侧边栏

| 项目 | 图标 | 激活态 | 非激活态 |
|------|------|--------|---------|
| 仪表盘 | `LayoutDashboard` | 蓝色文字 + 浅蓝背景 | 灰色文字 |
| 工作台 | `Wand2` | 蓝色文字 + 浅蓝背景 | 灰色文字 |
| 项目 | `FolderKanban` | 蓝色文字 + 浅蓝背景 | 灰色文字 |
| 画廊 | `Images` | 蓝色文字 + 浅蓝背景 | 灰色文字 |
| 存储管理 | `HardDrive` | 蓝色文字 + 浅蓝背景 | 灰色文字 |
| 计费 | `CreditCard` | 蓝色文字 + 浅蓝背景 | 灰色文字 |
| 设置 | `Settings` | 蓝色文字 + 浅蓝背景 | 灰色文字 |
| 管理（管理员） | `ShieldCheck` | 蓝色文字 + 浅蓝背景 | 灰色文字 |

- 激活态：`bg-primary/5 text-primary font-medium`
- 非激活态：`text-muted-foreground hover:bg-muted hover:text-foreground`
- 导航项高度：`40px`
- 圆角：`rounded-lg`
- 图标与文字间距：`12px`

### 14.2 顶部栏

- 左侧：Logo（翡翠绿图标 + 文字）
- 中间：搜索框（可选，宽度 320px）
- 右侧：通知图标 + 用户头像下拉菜单

---

## 15. 暗色模式（可选）

如需暗色模式，保留"深夜翡翠陈列室"风格：

| 用途 | 亮色值 | 暗色值 |
|------|--------|--------|
| 背景底色 | `hsl(0 0% 98%)` | `hsl(220 20% 10%)` |
| 卡片面 | `hsl(0 0% 100%)` | `hsl(220 20% 14%)` |
| 悬浮面 | `hsl(220 14% 96%)` | `hsl(220 20% 18%)` |
| 边框 | `hsl(220 13% 91%)` | `hsl(220 20% 20%)` |
| 主文字 | `hsl(224 71% 4%)` | `hsl(220 20% 96%)` |
| 次要文字 | `hsl(220 9% 46%)` | `hsl(220 10% 65%)` |
| 主色 | `hsl(221 83% 53%)` | `hsl(221 83% 60%)`（提亮） |
| 强调色 | `hsl(160 40% 40%)` | `hsl(160 50% 50%)`（提亮） |

实现方式：`.dark` 类切换，CSS 变量覆盖。

---

## 16. 设计禁忌

- 禁止使用深色/暗色背景作为主界面（暗色模式为可选功能）
- 禁止使用蓝紫色渐变（AI 产品滥用，缺乏辨识度）
- 禁止大面积霓虹/发光效果
- 禁止超过 3 种强调色（蓝 + 绿 + 语义色已足够）
- 禁止小于 12px 的字号
- 禁止无意义的动画（旋转、弹跳、脉冲）
- 禁止高饱和度背景色（干扰内容展示）
- 禁止使用填充图标（filled icon）
- 禁止用 emoji 替代功能图标
- 禁止硬编码 Hex/RGB 颜色（必须使用 CSS 变量）
- 禁止卡片默认带阴影（用边框区分，hover 才加阴影）
- 禁止导航激活态使用绿色（绿色仅用于"生成"操作和 Logo）

---

## 17. CSS 变量速查

以下变量已在 `src/index.css` 中定义，所有组件必须引用：

```css
:root {
  --background: hsl(0 0% 98%);
  --foreground: hsl(224 71% 4%);
  --card: hsl(0 0% 100%);
  --card-foreground: hsl(224 71% 4%);
  --popover: hsl(0 0% 100%);
  --popover-foreground: hsl(224 71% 4%);
  --primary: hsl(221 83% 53%);
  --primary-foreground: hsl(0 0% 100%);
  --secondary: hsl(220 14% 96%);
  --secondary-foreground: hsl(224 71% 4%);
  --muted: hsl(220 14% 96%);
  --muted-foreground: hsl(220 9% 46%);
  --accent: hsl(160 40% 40%);
  --accent-foreground: hsl(0 0% 100%);
  --destructive: hsl(0 84% 60%);
  --destructive-foreground: hsl(0 0% 100%);
  --border: hsl(220 13% 91%);
  --input: hsl(220 13% 91%);
  --ring: hsl(221 83% 53%);
  --surface-hover: hsl(220 14% 96%);
  --danger: hsl(0 84% 60%);
  --radius: 0.5rem;
}
```

Tailwind v4 映射通过 `@theme inline` 完成，使用语义类名（如 `bg-background`、`text-foreground`、`border-border`）而非直接引用 CSS 变量。
