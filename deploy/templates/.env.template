# CHANGE_ME: 部署前必须替换所有占位密钥（JWT_SECRET/DB 密码等），生成: openssl rand -base64 32
DATABASE_URL="postgresql://vibeai:vibeai123@127.0.0.1:5432/vibeai?sslmode=disable"
JWT_SECRET=<generated-by: openssl rand -base64 32>
S3_ENDPOINT_URL="http://localhost:9000"
S3_ACCESS_KEY="minioadmin"
S3_SECRET_KEY="minioadmin"
S3_BUCKET_NAME="vibeai"
STORAGE_PROVIDER="local"
PORT=3001
# Replicate API (未设置时 ReplicateAdapter 自动进入 Mock 模式)
# REPLICATE_API_TOKEN=r8_xxxxxxxxxxxxx
# REPLICATE_BASE_URL=https://api.replicate.com
